{
  "name": "nogasmlink-laptop-app",
  "version": "1.0.0",
  "private": true,
  "description": "Local companion app for NogasmLink - hosts the dashboard and controls The Handy over the ESP32's USB serial connection instead of WiFi.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@serialport/parser-readline": "^12.0.0",
    "express": "^4.19.2",
    "serialport": "^12.0.0",
    "ws": "^8.18.0"
  }
}
